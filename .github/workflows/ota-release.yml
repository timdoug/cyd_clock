name: OTA release binary

on:
  push:
    tags:
      - "r*"
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to upload to, for example r2026.06.15"
        required: true

permissions:
  contents: write

# Creating a release in the GitHub UI with a new r* tag fires both the tag
# push and the release-published triggers; serialize them so two runs don't
# race uploading the same assets.
concurrency:
  group: ota-release-${{ github.event.inputs.tag || github.event.release.tag_name || github.ref_name }}
  cancel-in-progress: false

jobs:
  build-and-upload:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Build esp32_2432s028 (default)
        uses: espressif/esp-idf-ci-action@v1
        with:
          esp_idf_version: v6.0.1
          target: esp32
          path: "."
          command: idf.py -B build -D CYD_BOARD=esp32_2432s028 build

      - name: Build esp32_2432s024
        uses: espressif/esp-idf-ci-action@v1
        with:
          esp_idf_version: v6.0.1
          target: esp32
          path: "."
          command: idf.py -B build_024 -D CYD_BOARD=esp32_2432s024 build

      - name: Prepare release assets
        run: |
          set -eu
          # cyd_clock.bin is the 028 app; the default OTA URL points at it. The
          # 024 app ships under a board-suffixed name. Bootloader, partition
          # table, and OTA data are board-independent, so one set covers both.
          # (Build outputs carry the board-suffixed project name that the OTA
          # identity check verifies; asset names stay as devices expect.)
          test -s build/cyd_clock-esp32_2432s028.bin
          test -s build_024/cyd_clock-esp32_2432s024.bin
          cp build/cyd_clock-esp32_2432s028.bin cyd_clock.bin
          cp build_024/cyd_clock-esp32_2432s024.bin cyd_clock-esp32_2432s024.bin
          cp build/bootloader/bootloader.bin bootloader.bin
          cp build/partition_table/partition-table.bin partition-table.bin
          cp build/ota_data_initial.bin ota_data_initial.bin

      - name: Resolve release tag
        id: release
        # Tag names reach the shell via env, not template substitution, so a
        # crafted tag cannot inject shell into a run with a write token.
        env:
          TAG_INPUT: ${{ github.event.inputs.tag }}
          TAG_RELEASE: ${{ github.event.release.tag_name }}
        run: |
          set -eu
          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ]; then
            tag="${TAG_INPUT}"
          elif [ "${GITHUB_EVENT_NAME}" = "release" ]; then
            tag="${TAG_RELEASE}"
          else
            tag="${GITHUB_REF_NAME}"
          fi
          echo "tag=${tag}" >> "${GITHUB_OUTPUT}"

      - name: Create release if needed
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.release.outputs.tag }}
        run: |
          set -eu
          gh release view "${TAG}" >/dev/null 2>&1 || \
            gh release create "${TAG}" --title "${TAG}" --notes "CYD Clock ${TAG}" --latest

      - name: Upload firmware assets
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.release.outputs.tag }}
        run: |
          set -eu
          gh release upload "${TAG}" \
            cyd_clock.bin \
            cyd_clock-esp32_2432s024.bin \
            bootloader.bin \
            partition-table.bin \
            ota_data_initial.bin \
            --clobber
